spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chatbot?characterEncoding=UTF-8
    username: root
    password: Mysqlwhgdk3*
  jpa:
    properties.hibernate:
      format_sql: true
    generate-ddl: true
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: create
    database: mysql